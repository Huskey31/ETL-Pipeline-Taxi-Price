# ETL-Pipeline-Taxi-Price
Automated ETL pipeline for taxi trip pricing data: extracts from CSV, cleans and transforms the data, handles missing values, and loads it into PostgreSQL. Designed for reliable, scheduled execution with logging and error handling
